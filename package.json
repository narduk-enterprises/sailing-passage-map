{
  "name": "nuxt-v4-template-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --filter web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "quality": "pnpm -r quality"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@parcel/watcher",
      "better-sqlite3",
      "core-js",
      "esbuild",
      "protobufjs",
      "sharp",
      "vue-demi",
      "workerd"
    ]
  }
}
